// const express = require("express");

// const router = express.Router();

// const messageController = require("../controller/message");

// const isAuth = require("../middleware/is-auth");

// router.post("/addmsg", isAuth, messageController.addMessage);

// router.post("/getmsg", isAuth, messageController.getAllMessage);

// router.delete("/delete/chats", isAuth, messageController.deleteChat);

// router.delete("/delete/chat/:cid", isAuth, messageController.deleteSingleMessage);

// module.exports = router;